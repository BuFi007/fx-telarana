{
  "name": "@fx-telarana/ponder",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "ponder dev --port ${PONDER_PORT:-42070}",
    "start": "ponder start",
    "build": "ponder codegen",
    "codegen": "ponder codegen",
    "typecheck": "ponder codegen && tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests",
    "lint": "ponder codegen && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fx-telarana/contracts": "workspace:*",
    "hono": "^4.12.7",
    "ponder": "^0.16.6",
    "viem": "^2.31.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
